import 'package:flutter/material.dart';

class AppColor {
  static const Color primaryDark = Color(0xFF061B57);
  static const Color primaryLight = Color(0xFF2777BE);
  static Color lightWhite = Colors.white.withOpacity(0.7);
  static Color lightWhiteMedium = Colors.white.withOpacity(0.15);
  static Color lightWhiteStrong = Colors.white.withOpacity(0.20);
  static const bgGradient = [primaryDark, primaryLight];
}